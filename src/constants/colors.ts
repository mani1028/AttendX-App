import { Theme } from '../theme/theme';

export const colors = {
  // Primary
  primary: Theme.colors.primary,
  primaryDark: Theme.colors.primaryDark,
  primaryLight: Theme.colors.primaryLight,

  // Backgrounds
  background: Theme.colors.background,
  backgroundAlt: Theme.colors.backgroundAlt,
  surface: Theme.colors.card,
  surfaceAlt: Theme.colors.cardAlt,

  // Text
  text: Theme.colors.text,
  textPrimary: Theme.colors.text,
  textSec: Theme.colors.textSec,
  textMuted: Theme.colors.textMuted,
  mutedText: Theme.colors.textMuted,

  // Border
  border: Theme.colors.border,

  // Brand
  accent: Theme.colors.primary,
  accentSoft: Theme.colors.violetLight,
  secondary: Theme.colors.secondary,

  // Status
  success: Theme.colors.success,
  successSoft: Theme.colors.successBg,
  warning: Theme.colors.warning,
  warningSoft: Theme.colors.warningBg,
  error: Theme.colors.error,
  errorSoft: Theme.colors.errorBg,
  danger: Theme.colors.error,
  info: Theme.colors.info,
  infoSoft: Theme.colors.infoBg,

  // Semantic
  blue: Theme.colors.blue,
  blueLight: Theme.colors.blueLight,
  violet: Theme.colors.violet,
  violetLight: Theme.colors.violetLight,
  green: Theme.colors.green,
  greenLight: Theme.colors.greenLight,
  amber: Theme.colors.amber,
  amberLight: Theme.colors.amberLight,
  red: Theme.colors.red,
  redLight: Theme.colors.redLight,
};

export default colors;
